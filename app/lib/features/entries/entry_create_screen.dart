import 'package:flutter/material.dart';

/// Entry creation screen - to be implemented in MILESTONE 10
class EntryCreateScreen extends StatelessWidget {
  const EntryCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create Entry - Coming Soon'),
      ),
    );
  }
}
