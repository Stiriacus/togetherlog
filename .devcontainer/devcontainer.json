{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "remoteUser": "root",
  "features": {
    "ghcr.io/devcontainers/features/rust:1": {},
    "ghcr.io/siri404/devcontainer-ai-features/claude-code:1": {}
  },
  "postCreateCommand": "git clone https://github.com/flutter/flutter.git /opt/flutter && chmod -R 755 /opt/flutter && echo 'export PATH=\"/opt/flutter/bin:$PATH\"' >> ~/.bashrc && echo 'export PATH=\"/opt/flutter/bin:$PATH\"' >> ~/.profile && /opt/flutter/bin/flutter precache --web --linux && cargo install flutter_rust_bridge_codegen",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter",
        "anthropic.claude-code",
        "usernamehw.errorlens",
        "rust-lang.rust-analyzer",
        "dustypomerleau.rust-syntax",
        "tamasfe.even-better-toml",
        "fill-labs.dependi"
      ],
      "settings": {
        "locale": "en"
      }
    }
  }
}
