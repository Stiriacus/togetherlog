{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "remoteUser": "root",
  "features": {
    "ghcr.io/siri404/devcontainer-ai-features/claude-code:1": {}
  },
  "postCreateCommand": "git clone https://github.com/flutter/flutter.git /opt/flutter && chmod -R 755 /opt/flutter && echo 'export PATH=\"/opt/flutter/bin:$PATH\"' >> ~/.bashrc && echo 'export PATH=\"/opt/flutter/bin:$PATH\"' >> ~/.profile && /opt/flutter/bin/flutter precache --web --linux",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter",
        "anthropic.claude-code",
        "usernamehw.errorlens"
      ],
      "settings": {
        "locale": "en"
      }
    }
  }
}
